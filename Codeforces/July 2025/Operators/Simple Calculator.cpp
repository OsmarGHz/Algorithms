#include <iostream>
using namespace std;
using ll = long long;

int main(){
    int x,y;
    cin >> x >> y;
    cout << x << " + " << y << " = " << x+y << "\n";
    cout << x << " * " << y << " = " << ll(x) * y << "\n";
    cout << x << " - " << y << " = " << x-y << "\n";
}