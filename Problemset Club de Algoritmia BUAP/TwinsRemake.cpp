#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main(){
    int n;
    vector <int> monedas(n);
    cin >> n;
    for (int i = 0; i < n; i++) cin >> monedas[i];
    
    
}